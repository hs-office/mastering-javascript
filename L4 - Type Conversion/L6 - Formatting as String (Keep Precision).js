// toFixed(N) (String Output)
let a = 2.4567;
let fixedStr = a.toFixed(2); // "2.46" (string)

// toPrecision(N) (Significant Digits)
let b = 2.4567;
let preciseStr = b.toPrecision(3); // "2.46" (string)


