/*
 unshift() → Adds item to the start of an array
 What it does:
 * It adds one or more items to the beginning of the array
   and returns the new length of the array.
 */

let colors = ['green', 'blue']
colors.unshift('red')

console.log(colors)         // ['red', 'green', 'blue']
