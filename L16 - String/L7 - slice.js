/*
 It returns a portion of the string, based on the
 start and (optional) end positions.
 */
let a = 'hello'
console.log(a.slice(0,4))       // hell
console.log(a.slice(0,2))       // he
console.log(a.slice(-4,-1))     // ell
